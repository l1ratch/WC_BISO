# 📌 Лекция №2 — Файловые системы и СУБД

## 🔑 Основные термины

* **Файл** – именованная область внешней памяти.
* **Файловая система (ФС)** – управляет размещением, именованием и доступом к файлам.
* **СУБД** – программный комплекс для создания, хранения, модификации и поиска данных с обеспечением их целостности.

---

## 📂 Файловые системы

**Функции ФС:**

* распределение внешней памяти;
* многоуровневое именование (каталоги/папки);
* авторизация доступа:

  * **мандатный** – индивидуальные права каждого пользователя;
  * **дискреционный** – права для групп (чтение, запись, выполнение).

**Проблемы ФС:**

* слабая структуризация данных (структура на совести приложений);
* сложность многоключевого доступа;
* избыточность данных;
* необходимость вручную поддерживать **согласованность** при изменении связанных файлов;
* блокировка файлов при параллельной работе.

---

## 🗄️ Базы данных и СУБД

**Переход от ФС к БД вызван потребностями:**

* хранить **структурированные** данные;
* выполнять сложные выборки и вычисления;
* поддерживать **целостность** данных;
* обеспечивать **параллельный доступ** и восстановление после сбоев.

**Ключевые возможности СУБД:**

1. **Целостность данных** (ограничения и правила).
2. **Транзакции** – атомарность, изоляция, сохранение результата.
3. **Контроль избыточности** (нормализация данных).
4. **Язык запросов (SQL)** – DDL, DML.
5. **Независимость от данных** (физическая и логическая).
6. **Параллельная работа** (механизмы блокировок).
7. **Резервное копирование и восстановление**.
8. **Безопасность** – идентификация и разграничение прав.
9. **Повышение производительности** – готовые функции для программистов.

---

## 🏗 Архитектура БД (ANSI-SPARC)

**Три уровня:**

* **Внешний** – представление данных для конкретного пользователя/приложения.
* **Концептуальный** – логическая структура всей БД (сущности, атрибуты, связи, ограничения).
* **Внутренний** – физическая организация данных на носителях.

**Независимость данных:**

* **Логическая** – изменения концептуальной схемы не затрагивают приложения.
* **Физическая** – перенос на новые устройства без переписывания программ.

---

## ⚙️ Обобщенная архитектура СУБД

**Основные компоненты:**

* **Модуль управления внешней памятью** – хранение данных, индексы.
* **Модуль управления буферами** – работа с оперативной памятью.
* **Процессор запросов**:

  * компилятор (синтаксический, семантический анализ, оптимизация запроса);
  * исполняющая машина (выполнение плана запроса).
* **Модуль транзакций** – планировщик заданий (блокировки, deadlock-менеджмент), модуль протоколирования и восстановления (журналы, откат).

---

## 📑 Логические структуры БД

* **Таблицы** – базовые структуры данных (строки и столбцы).
* **Представления** – виртуальные таблицы, упрощают доступ и обеспечивают безопасность.
* **Индексы:**

  * B\*-деревья, реверсированные, битовые;
  * **кластеризованные** (сортируют строки на диске) и **некластеризованные** (только ссылки).
* **Последовательности** – генерация уникальных значений.
* **Хранимые процедуры, функции, триггеры** – бизнес-логика на стороне БД.
* **Кластеры и хешированные кластеры** – физическая оптимизация хранения.

---

## 🎯 Вывод

СУБД обеспечивает:

* надежное долговременное хранение данных;
* многопользовательский доступ;
* автоматическую поддержку целостности и восстановление после сбоев;
* удобный язык запросов и высокую производительность.
